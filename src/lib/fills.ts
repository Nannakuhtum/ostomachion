/** Piece fills, cycled by piece index. Shared by the board pieces and the
 * collection's mini solutions so both render the same ceramic object. */
export const FILLS = [
  'var(--cream-slip)',
  'var(--ochre)',
  'var(--terracotta-deep)',
  'var(--terracotta)',
] as const;
